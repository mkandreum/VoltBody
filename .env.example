# Frontend
# En desarrollo con Vite puedes dejarlo vacio y usar el proxy /api.
# En despliegues separados, indica la URL base del backend.
VITE_API_URL=

# Backend
NODE_ENV=production
PORT=3000
SESSION_DAYS=30
DATABASE_URL=postgresql://voltbody:voltbody123@localhost:5432/voltbody?schema=public
